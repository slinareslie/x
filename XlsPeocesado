Sort(
    Distinct(
        AddColumns(
            [@'DirectorioTransportistaSIGAD'];
            "Paises";
            If(Len(PA) = 2; PA; "Otros")
        );
        Paises
    );
    Result;
    Ascending
)
